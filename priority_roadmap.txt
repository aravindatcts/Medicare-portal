PRIORITY ROADMAP

SPRINT 1 — Security & Stability
  □ Re-enable Descope AuthProvider + ProtectedRoute on all routes
  □ Add Zod validation to all API responses in dashboardService
  □ Set VITE_API_URL via .env, remove hardcoded localhost
  □ Add CSP headers via vite-plugin-html
  □ Wrap each page in an ErrorBoundary

SPRINT 2 — Performance
  □ React.lazy() + Suspense on all route components
  □ Cache db.json in-memory on mock server startup
  □ Remove the 2-second artificial delay
  □ Extract DoctorAvatar + SPECIALTY_THEME to packages/ui
  □ Replace onMouseEnter style mutations with CSS :hover classes

SPRINT 3 — Accessibility
  □ Add aria-label to all icon-only buttons
  □ Add role="switch" + aria-checked to all toggles
  □ Add role="menu" + keyboard handling to TopNav dropdown
  □ Add @media (prefers-reduced-motion: reduce) to all animations
  □ Replace px font sizes with rem in App.module.css

SPRINT 4 — Design System Consolidation
  □ Migrate inline color overrides to Tailwind tokens
  □ Tokenize shadows (sm/md/lg/xl) in tailwind.config
  □ Adopt pageWrap on remaining pages (Prescriptions)
  □ Define one rule: Tailwind for component-level, CSS modules for layout-level only
  □ Move REVIEWS and prescription data to mock server endpoints

SPRINT 5 — Scalability
  □ Create packages/ui with shared components
  □ Add Vitest + Testing Library
  □ Set up GitHub Actions CI pipeline
  □ Enable strict: true in tsconfig